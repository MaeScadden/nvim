return function(use)
  -- automatically resize windows
  --[[
  use {
    "beauwilliams/focus.nvim",
    config = function()
      require("focus").setup()
    end
  }
  --]]

  -- shade splits that you are not currently in
  
end
