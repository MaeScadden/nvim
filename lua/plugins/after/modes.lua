require('modes').setup({
  colors = {
    copy = "#9745be",
    delete = "#c75c6a",
    insert = "#78dcc5",
    visual = "#f5c359",
  },

  -- Cursorline highlight opacity
  line_opacity = 0.1,

  -- Highlight cursor
  set_cursor = true,

  -- Highlight in active window only
  focus_only = false
})
